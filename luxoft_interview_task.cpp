#include <iostream>
using namespace std;

// there are 26 letters A-Z
// we need to generate password with arbitrary length

string generate_password(size_t len)
{
	return "";
}

int main(int argc, char[] argv)
{
	cout << "new password: " << generate_password(8);
	
	return 0;
}
